import React, {Component} from 'react';
import BookList from './BookList'

/*class Bookshelves extends Component{


   
    render(){

    const {books}=this.props
        const currentlyReading = books.filter(book=>book.shelf === 'currentlyReading');
        const wantRead = books.filter(book=>book.shelf === 'wantToRead');
        const alreadyRead = book.filter(book=>book.shelf==='read');




        return(
            <div>
                <h1 className="bookshelf-title">Currently Reading</h1>
                <div>{currentlyReading.map(book=>(
                    <BookList books={books}/>
                ))}
                </div>
                <h1 className="bookshelf-title">Want to Read</h1>
                <div>{wantRead.map(book=>(
                    <BookList books={books}/>
                ))}
                </div>
                <h1 className="bookshelf-title">Already Read</h1>
                <div>{alreadyRead.map(book=>(
                    <BookList books={books}/>
                ))}
                </div>
               
               
            </div>
        )

    }

}







export default Bookshelves*/